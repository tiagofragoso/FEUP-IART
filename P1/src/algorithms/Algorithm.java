package algorithms;

import graph.Graph;

public class Algorithm {
    private boolean debug = false;
    protected Graph graph;

    public Algorithm(Graph graph) {
        this.graph = graph;
    }
}
